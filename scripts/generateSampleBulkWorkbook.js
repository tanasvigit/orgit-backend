/**
 * @deprecated Use the exact template generator instead:
 *   npm run generate:bulk-sample
 * (calls scripts/generateExactBulkSample.ts → OrgIt_Master_Bulk_SAMPLE.xlsx)
 */
console.error(
  'generateSampleBulkWorkbook.js is deprecated. Run: npm run generate:bulk-sample'
);
process.exit(1);
